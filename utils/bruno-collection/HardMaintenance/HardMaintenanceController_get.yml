info:
  name: HardMaintenanceController_get
  type: http
  seq: 1
  tags:
    - HardMaintenance

http:
  method: GET
  url: "{{baseUrl}}/hardMaintenance"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: https://api.lynqa.smartesting.com

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    request:
      url: "{{baseUrl}}/hardMaintenance"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
