info:
  name: Get formatted Changelog
  type: http
  seq: 2
  tags:
    - Changelog

http:
  method: GET
  url: "{{baseUrl}}/changelog/formatted"
  params:
    - name: releasesCount
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Changelog formatted
    request:
      url: "{{baseUrl}}/changelog/formatted"
      method: GET
      params:
        - name: releasesCount
          value: ""
          type: query
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
