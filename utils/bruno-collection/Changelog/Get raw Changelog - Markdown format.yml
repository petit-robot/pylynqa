info:
  name: Get raw Changelog - Markdown format
  type: http
  seq: 1
  tags:
    - Changelog

http:
  method: GET
  url: "{{baseUrl}}/changelog/raw"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Changelog in markdown format
    request:
      url: "{{baseUrl}}/changelog/raw"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
